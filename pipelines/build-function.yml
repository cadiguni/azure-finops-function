trigger: none

variables:
  - template: config.yml

pool: azp-gvdasa-be-01

stages:
  - stage: 'Build_function'
    displayName: 'Build function'
    jobs:
    - job: 'Build'
      displayName: 'Build'

      steps:
      - task: NuGetToolInstaller@1
        displayName: 'Instalar Nuget'

      - task: DotNetCoreCLI@2
        displayName: Restore
        inputs:
          command: restore
          feedsToUse: 'select'
          vstsFeed: 'Feed_NAP'
          includeNuGetOrg: true
          projects: |
            src/Gvdasa.FinOpsApi.WebApi/Gvdasa.FinOpsApi.WebApi.csproj
            src/Gvdasa.FinOpsApi.Domain/Gvdasa.FinOpsApi.Domain.csproj
            src/Gvdasa.FinOpsApi.Infra/Gvdasa.FinOpsApi.Infra.csproj
            src/Gvdasa.FinOpsApi.Modelos/Gvdasa.FinOpsApi.Modelos.csproj

      - task: DotNetCoreCLI@2
        displayName: Build
        inputs:
          arguments: '--configuration Release'
          feedsToUse: 'select'
          vstsFeed: 'Feed_NAP'
          includeNuGetOrg: true
          projects: |
            src/Gvdasa.FinOpsApi.WebApi/Gvdasa.FinOpsApi.WebApi.csproj
            src/Gvdasa.FinOpsApi.Domain/Gvdasa.FinOpsApi.Domain.csproj
            src/Gvdasa.FinOpsApi.Infra/Gvdasa.FinOpsApi.Infra.csproj
            src/Gvdasa.FinOpsApi.Modelos/Gvdasa.FinOpsApi.Modelos.csproj

      - task: DotNetCoreCLI@2
        displayName: Publish
        inputs:
          command: publish
          publishWebProjects: false
          feedsToUse: 'select'
          vstsFeed: 'Feed_NAP'
          includeNuGetOrg: true
          projects: 'src/Gvdasa.FinOpsApi.WebApi/Gvdasa.FinOpsApi.WebApi.csproj'
          arguments: '--configuration Release --output $(build.artifactstagingdirectory)'

      - task: PublishBuildArtifacts@1
        displayName: 'Publish Artifact'
        inputs:
          PathtoPublish: '$(build.artifactstagingdirectory)'
          ArtifactName: '${{ variables.ArtifactName }}'